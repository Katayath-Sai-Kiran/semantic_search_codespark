name: semantic_search_codespark
description: >-
  On-device semantic & vector search for Flutter — match by meaning, fully
  offline, no API keys. AI-powered search with a three-line API.
version: 0.2.0
repository: https://github.com/Katayath-Sai-Kiran/semantic_search_codespark
homepage: https://ksaikiran.dev
issue_tracker: https://github.com/Katayath-Sai-Kiran/semantic_search_codespark/issues
topics:
  - semantic-search
  - search
  - embeddings
  - ai
  - offline

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  ai_core_codespark: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
